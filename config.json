{
  "_comment_global": "本配置文件包含业务逻辑相关的配置项，环境相关和敏感信息请在 .env 文件中配置",
  "_comment_env_override": "优先级为 defaults < config.json 显式值 < FUNASR_PROFILE < FUNASR_* env；大部分配置项都可以通过环境变量覆盖，详见 .env.example 文件说明",

  "server": {
    "_comment_host_port": "服务器地址和端口请在 .env 中配置: FUNASR_SERVER_HOST, FUNASR_SERVER_PORT",
    "_comment_directories": "所有目录路径请在 .env 中配置: FUNASR_UPLOAD_DIR, FUNASR_TEMP_DIR 等",

    "max_connections": 100,
    "_comment_max_connections": "可通过环境变量覆盖: FUNASR_SERVER_MAX_CONNECTIONS",

    "max_file_size_mb": 5000,
    "_comment_max_file_size_mb": "可通过环境变量覆盖: FUNASR_SERVER_MAX_FILE_SIZE_MB",

    "allowed_extensions": [
      ".wav",
      ".mp3",
      ".mp4",
      ".m4a",
      ".flac",
      ".aac",
      ".ogg",
      ".opus",
      ".webm"
    ],

    "connection_timeout_seconds": 300,
    "heartbeat_interval_seconds": 60
  },

  "qwen3": {
    "_comment_engine": "Qwen3-Diarize 引擎配置(仅当 transcription.default_engine == 'qwen3' 时生效)",
    "_comment_models": "模型路径由 scripts/download_qwen3_models.sh 落地, 也可通过 FUNASR_QWEN3_* 环境变量覆盖",

    "asr_model_dir": "./models/qwen3_diarize/Qwen3-ASR-1.7B",
    "segmentation_model": "./models/qwen3_diarize/sherpa/pyannote-segmentation-3.0/model.onnx",
    "embedding_model": "./models/qwen3_diarize/sherpa/nemo-titanet-small/embedding.onnx",

    "_comment_preset": "PoC 报告 preset=auto 普适最优: fp32 segmentation + NeMo TitaNet + cpu + runtime-aware threads + 自适应聚类",
    "num_speakers": null,
    "cluster_threshold": 0.9,
    "num_threads": 8,
    "provider": "cpu",

    "language": "Chinese",
    "temperature": 0.4
  },

  "funasr": {
    "_comment_device": "设备配置请在 .env 中配置: FUNASR_DEVICE, FUNASR_DEVICE_PRIORITY",

    "model": "paraformer-zh",
    "model_revision": "v2.0.4",

    "vad_model": "fsmn-vad",
    "vad_model_revision": "v2.0.4",

    "punc_model": "ct-punc-c",
    "punc_model_revision": "v2.0.4",

    "spk_model": "cam++",
    "spk_model_revision": "v2.0.2",

    "_comment_model_dir": "模型目录请在 .env 中配置: FUNASR_MODEL_DIR",

    "batch_size_s": 60,
    "_comment_batch_size_s": "可通过环境变量覆盖: FUNASR_BATCH_SIZE_S",

    "ncpu": 16,
    "_comment_ncpu": "可通过环境变量覆盖: FUNASR_NCPU",

    "fallback_on_error": true,
    "disable_update": true,
    "disable_pbar": true
  },

  "transcription": {
    "max_concurrent_tasks": 2,
    "_comment_max_concurrent_tasks": "可通过环境变量覆盖: FUNASR_MAX_CONCURRENT_TASKS",

    "max_queue_size": 150,
    "_comment_max_queue_size": "队列上限=准入控制。此处 config.json 显式值 150 按优先级生效；FUNASR_PROFILE 或 FUNASR_MAX_QUEUE_SIZE 可在更高优先级覆盖，不与代码默认值擅自统一。异步客户端应在 task_queued/upload_complete 后用 task_status_batch 轮询，queue_full 按 retry_after 退避重试。",

    "concurrency_mode": "pool",

    "task_timeout_minutes": 30,
    "_comment_task_timeout_minutes": "可通过环境变量覆盖: FUNASR_TASK_TIMEOUT_MINUTES",

    "_comment_queue_resilience": "高负载队列健壮性字段(默认见 config.py，可 env 覆盖): task_retention_ttl_seconds(终态任务保留TTL=轮询窗), task_max_retained(self.tasks硬上限), task_max_processing_seconds(看门狗卡死阈值), upload_session_ttl_seconds/upload_session_max_count(分片session防泄漏)。详见 docs/开发/2026-06-16-高负载队列机制-止血修复计划.md",

    "retry_times": 2,
    "cache_enabled": true,
    "delete_after_transcription": true,
    "transcription_speed_ratio": 10,
    "_comment_transcription_speed_ratio": "转录速度比率（RTF的倒数），不同设备性能不同，可通过环境变量覆盖: FUNASR_TRANSCRIPTION_SPEED_RATIO",
    "queue_status_enabled": true,

    "qwen3_pool_size": 3,
    "_comment_qwen3_pool_size": "Qwen3 引擎专用 worker pool 大小。此处 config.json 显式值 3 按优先级生效；profile/env 可在更高优先级覆盖，实际值按显存与并发需求设置。独立于 max_concurrent_tasks。可通过环境变量覆盖: FUNASR_QWEN3_POOL_SIZE"
  },

  "database": {
    "_comment_path": "数据库路径通过 .env 中的 FUNASR_DATA_DIR 构建",
    "max_cache_days": 30
  },

  "notification": {
    "_comment_enabled": "是否启用通知请在 .env 中配置: FUNASR_NOTIFICATION_ENABLED",
    "_comment_webhook_url": "企业微信 Webhook URL 请在 .env 中配置: FUNASR_WEBHOOK_URL (必填)",

    "retry_times": 3,
    "timeout_seconds": 10
  },

  "auth": {
    "_comment_enabled": "是否启用认证请在 .env 中配置: FUNASR_AUTH_ENABLED",
    "_comment_secret_key": "JWT 密钥请在 .env 中配置: FUNASR_AUTH_SECRET_KEY (必填)",

    "algorithm": "HS256",
    "access_token_expire_minutes": 1440
  },

  "logging": {
    "_comment_level": "日志级别请在 .env 中配置: FUNASR_LOG_LEVEL",
    "_comment_log_dir": "日志目录请在 .env 中配置: FUNASR_LOG_DIR",

    "format": "<green>{time:YYYY-MM-DD HH:mm:ss.SSS}</green> | <level>{level: <8}</level> | <cyan>{name}</cyan>:<cyan>{function}</cyan>:<cyan>{line}</cyan> - <level>{message}</level>",
    "rotation": "100 MB",
    "retention": "7 days"
  }
}
