# R2 主脑分诊与交付边界 [codex]

固定审查范围为 `43e8e949d8a860cdc9ab0b58e50a20f279d90861..a24d79332761f92c330d388123aef85390e67234`。独立 R2 原始 FAIL 保留，本文件给出实际差异与消费环境下的处置。

| 意见 | 工具标注 | 本批处置 | 核对依据 |
|---|---|---|---|
| 用户 API 取消不终止推理，迟到结果覆盖取消状态 | P1 | 确认存量行为，列为 API 取消专项 backlog；不成立为本批新增 P1，不在本批扩展 TaskManager 状态机制。 | `git diff --exit-code base H0 -- src/core/task_manager.py src/api/websocket_handler.py src/core/database.py` 返回 0、差异为空；API cancel 与 `_process_task` 完成通知逻辑均未改。设计第 24 行为“池调用超时、取消”，第 53 行明确是“pool timeout/crash/cancel/shutdown”；测试锁定 asyncio 协程取消和服务停机，不承诺把用户 cancel API 接到池。 |
| reap 失败保留 lease 后，上层重试可能等待不可用 slot | P2 | 接受不修，记录资源回收失败后错误分类改进项。 | 实证来自注入 OS reap 失败，未在真实 Mac 发生；真实部署池大小 3，一个保留 lease 不等于整个池无可用 slot。正常超时/崩溃/协程取消的真实 subprocess 回收测试通过，Mac 候选全部已记录 PID 已退出；在尚未证实退出时保留资源仍是必要约束。 |

这里不否认旧 API 取消行为值得修复，也不把它说成已修复。它需要单独定义对排队、运行、缓存命中、文件删除和通知的终态契约；本次将其接入会新增每任务协程状态并扩大用户语义，不能用池层单点补丁代替设计。

## 对照本轮目标的审计

- 方案仍必要：生产实际出现池超时后旧进程仍存活，以及直接写最终 pickle 的读取错误；旧代码上的 producer 红验失败，新代码通过。这两项不依赖取消接口的语义。
- 里程碑真完成了吗：实现、固定 H0 测试及 Mac 候选 37 分钟强制重算通过；生产更新与生产真实入口验收尚未执行，当前不宣告总目标完成。
- 下一个目标还是对的吗：继续正常 PR 合并、Mac 生产更新和实际长请求验收；不更换模型、依赖或放宽超时。
- 有没有漏掉的里程碑：用户 API 取消端到端终态是存量专项，不作为本批池超时/原子发布的新增阶段；已在此记录，不承诺本批交付它。
- 新证据是否改变工作顺序：Mac 全集成失败的 18 项在同环境原主干全部复现（缺 sherpa_onnx/gguf）；生产 FunASR parity、入口与长音频通过，保持先交付当前修复的顺序。
- done 定义还成立吗：须核实生产确切 SHA、旧 worker 退出、新池三 worker 健康、强制重算长音频成功、私有目录使用/回收；不把候选成功等同于已经部署。

## 审查证据与结论

R1 从 producer/正向调用检查，R2 从取消/失败与下层资源检查；R2 独立复跑 24+37 项定向测试并使用真实 subprocess 探针。其 OCR 为 partial，部分复核读取了 base，未被当作完整通过依据。

两轮没有确认本批新增 P1；原始工具 FAIL 与本分诊同时留存。接受本批进入合并与生产验收，前提是实际候选证据和部署后的检查完整。用户 API cancel 的存量限制与 Qwen3 缺依赖的测试限制必须保留在交付说明中。
