# 部署指南

> **Mac Studio 是当前生产目标**：使用 `mac_prod`（FunASR）。Linux CUDA 是受支持的部署路径：使用 `cuda_prod` / `cuda_dev`（Qwen3）。不要把当前生产目标误读为仅支持 macOS。

---

## 一、环境要求

| 项 | 要求 |
|---|---|
| 操作系统 | 生产目标：macOS 13+ (Apple Silicon Mac Studio)；支持路径：Linux + NVIDIA CUDA |
| Python | 3.12.x(CapsWriter/qwen_asr_gguf 引擎要求, FunASR parity 已验证 3.12 兼容) |
| 内存 | Mac FunASR 按并发需求配置；Qwen3 GGUF + Metal 约 3.8GB RSS 起步 |
| 磁盘 | 至少 8GB(FunASR 模型 ~1.2GB + Qwen3 模型 ~2.1GB + 缓存数据库 + 日志) |
| 依赖 | FFmpeg（`brew install ffmpeg`） |
| 进程管理 | PM2（**仅 prod 需要**：`npm install -g pm2`） |
| FunASR 模型 | ModelScope 自动下载(~1.2GB), 存 `~/.cache/modelscope/hub/`；Mac 生产路径使用 |
| Qwen3 模型 | `scripts/download_qwen3_models.sh` 落地到 `models/qwen3_diarize/`(~2.1GB, 仅 default_engine=qwen3 需要; 含 Qwen3-ASR-1.7B GGUF + sherpa pyannote-segmentation + sherpa nemo-titanet-small embedding ~30MB, PR4 cluster_merge 复用同一 embedding 模型, 不额外占空间) |

---

## 二、prod/dev 物理隔离布局

| 维度 | prod | dev |
|---|---|---|
| 目录 | `~/Production/funasr_spk_server/` | `~/Dev/projects/250729_funasr_spk_server/funasr_spk_server/` |
| 端口 | 8767 | 8867 |
| Git 分支 | `main` | `dev`（合并后推 main） |
| Python venv | 项目内 `venv/`（独立） | 项目内 `venv/`（独立） |
| 进程守护 | **PM2**（`funasr-server`） | **无（前台直跑）** |
| 日志级别 | INFO | DEBUG |
| 通知 | 启用企微 webhook | 关闭 |
| 认证 | 启用 | 关闭 |

模型文件不需要复制 — `~/.cache/modelscope/` 是用户级全局缓存，prod/dev 自动共享。

---

## 三、生产部署（prod）

### 首次部署
```bash
cd ~/Production/
git clone git@github.com:zj1123581321/funasr_spk_server.git
cd funasr_spk_server
git checkout main

# 创建 venv（推荐 uv，也可用 python -m venv）
python3.12 -m venv venv  # 或 uv venv venv --python 3.12
venv/bin/pip install --upgrade pip
venv/bin/pip install -r requirements.txt

# 配置 .env（Mac Studio 生产建议：FUNASR_PROFILE=mac_prod；再配置 webhook、认证密钥）
cp .env.example .env
vim .env  # FUNASR_PROFILE=mac_prod, FUNASR_AUTH_SECRET_KEY=..., FUNASR_WEBHOOK_URL=...

# (可选) 落地 Qwen3 模型权重 — 仅 FUNASR_DEFAULT_ENGINE=qwen3 时需要
bash scripts/download_qwen3_models.sh   # 优先从 ~/Production/qwen_asr_server/models 复制

# 用 PM2 启动(ecosystem.config.cjs 已在仓库里)
pm2 start ecosystem.config.cjs
pm2 save                    # 保存进程列表，开机自动恢复
pm2 startup                 # 输出一条 sudo 命令，执行后开机自启
```

### 日常更新

Mac Studio 生产更新必须在同一个 shell 中按下面流程执行；任一步失败都会停止，只有 doctor 和验收全部通过才会重启服务。

```bash
set -euo pipefail
cd ~/Production/funasr_spk_server

# 1) 配置门禁：只接受 Mac 生产 profile 和 FunASR；发现 qwen3 立即停止，不自动修改 .env
test -f .env
profile_count="$(awk '/^[[:space:]]*FUNASR_PROFILE[[:space:]]*=/{count++} END{print count+0}' .env)"
test "$profile_count" -eq 1
profile="$(awk -F= '/^[[:space:]]*FUNASR_PROFILE[[:space:]]*=/{v=$2; sub(/^[[:space:]]*/, "", v); sub(/[[:space:]]*$/, "", v); print v; exit}' .env)"
test "$profile" = mac_prod
bad_engine="$(awk -F= '/^[[:space:]]*FUNASR_DEFAULT_ENGINE[[:space:]]*=/{v=$2; sub(/^[[:space:]]*/, "", v); sub(/[[:space:]]*$/, "", v); if (v != "" && v != "funasr") {print v; exit}}' .env)"
test -z "$bad_engine"

# 2) 记录旧版本并创建本地、唯一、可用的回滚分支；不覆盖或删除任何数据
test -z "$(git status --porcelain)"
OLD_SHA="$(git rev-parse HEAD)"
ROLLBACK_BRANCH="rollback/mac-prod-${OLD_SHA}-$(date +%Y%m%d%H%M%S)-$$"
git branch "$ROLLBACK_BRANCH" "$OLD_SHA"
test "$(git rev-parse "$ROLLBACK_BRANCH")" = "$OLD_SHA"
echo "OLD_SHA=$OLD_SHA ROLLBACK_BRANCH=$ROLLBACK_BRANCH"

# 3) 只允许快进更新
git pull --ff-only origin main
venv/bin/pip install -r requirements.txt   # 仅 requirements.txt 有变化时

# 4) doctor: 0 继续；1 打印并人工审阅后输入 CONTINUE；2 或其他值阻断
doctor_rc=0
doctor_json="$(venv/bin/python scripts/doctor.py --json)" || doctor_rc=$?
printf '%s\n' "$doctor_json"
case "$doctor_rc" in
  0) ;;
  1) printf 'doctor 有 warnings；审阅上面的 JSON 后输入 CONTINUE 继续：'
     read -r doctor_confirmation
     test "$doctor_confirmation" = CONTINUE ;;
  2) echo 'doctor 返回 2，阻断重启' >&2; exit 2 ;;
  *) echo "doctor 返回未知退出码: $doctor_rc，阻断重启" >&2; exit 1 ;;
esac

# 5) doctor JSON 的 effective engine 必须仍是 FunASR
printf '%s\n' "$doctor_json" | venv/bin/python -c 'import json,sys; report=json.load(sys.stdin); engine=report.get("engine"); print(f"doctor effective engine={engine}"); raise SystemExit(0 if engine == "funasr" else 1)'

# 6) 以上全部通过后才重启，并刷新 PM2 环境
pm2 restart funasr-server --update-env
curl -fsS http://127.0.0.1:8767/health
curl -fsS http://127.0.0.1:8767/capabilities
pm2 logs funasr-server --lines 50 --nostream

# 7) 真实 FunASR 小音频验收；未提供路径时停止
: "${SMOKE_AUDIO:?请先设置 SMOKE_AUDIO 为真实短音频路径}"
test -f "$SMOKE_AUDIO"
venv/bin/python examples/transcribe_media.py "$SMOKE_AUDIO" --server ws://127.0.0.1:8767
```

验收失败时，使用上一步输出的完整 `ROLLBACK_BRANCH` 值恢复代码；这是切换分支，不使用 `git reset --hard`：

```bash
set -euo pipefail
cd ~/Production/funasr_spk_server
ROLLBACK_BRANCH="__ROLLBACK_BRANCH_FROM_OUTPUT__"
test "$ROLLBACK_BRANCH" != "__ROLLBACK_BRANCH_FROM_OUTPUT__"
git show-ref --verify --quiet "refs/heads/$ROLLBACK_BRANCH"
git switch "$ROLLBACK_BRANCH"
pm2 restart funasr-server --update-env
curl -fsS http://127.0.0.1:8767/health
```

### doctor 只读诊断

从仓库任意当前工作目录运行：

```bash
venv/bin/python /绝对路径/funasr_spk_server/scripts/doctor.py --json
# 或在仓库内：python scripts/doctor.py --json
```

命令只接受 `--json`，stdout 为单行 JSON；退出码 `0` 表示正常，`1` 表示有警告但可继续，`2` 表示配置/工件/目录错误。它只读取配置、provider 能力、模型工件元数据和目录可用性，不创建目录、不写配置、不启动/重启服务、不读取或回显模型内容与原始路径。doctor 通过脚本自身位置定位仓库，因此与当前 cwd 无关。

### 状态查询 / 重启 / 停止
```bash
pm2 status                  # 看进程状态
pm2 logs funasr-server      # 实时日志
pm2 restart funasr-server --update-env   # 重启
pm2 stop funasr-server      # 停止（不删除）
pm2 delete funasr-server    # 删除进程
```

### 可观测性（同端口 /health + /metrics）
服务在 WebSocket 同端口暴露只读 HTTP 端点（零额外端口，零新依赖）：
```bash
curl http://127.0.0.1:8767/health      # 存活探针: 200 healthy / 503 degraded (裸放, 无需 token)
# 浏览器打开 http://<prod-LAN-IP>:8767/  →  极简状态页 (每 3s 自刷)
```
⚠️ **prod 绑 `0.0.0.0`，A5 安全要求 `/metrics` 必须设 token**，否则拒绝（防全网段裸暴露聚合指标）：
```bash
# .env 加: FUNASR_METRICS_TOKEN=<强随机串>  然后 pm2 restart funasr-server
curl -H "Authorization: Bearer <token>" http://127.0.0.1:8767/metrics
# 或浏览器: http://<prod-LAN-IP>:8767/?token=<token>   (状态页指标区才显示)
```
端点设计与指标清单见 `docs/开发/2026-06-16-可观测性仪表盘与测试加固-设计定案与落地计划.md`。

---

## 四、开发运行（dev，不挂 PM2）

### 首次拉起开发环境
```bash
cd ~/Dev/projects/250729_funasr_spk_server/
git clone git@github.com:zj1123581321/funasr_spk_server.git
cd funasr_spk_server
git checkout dev

python3.12 -m venv venv  # 或 uv venv venv --python 3.12
venv/bin/pip install --upgrade pip
venv/bin/pip install -r requirements.txt

cp .env.example .env
vim .env  # FUNASR_SERVER_PORT=8867, FUNASR_LOG_LEVEL=DEBUG, 关闭认证/通知

# 前台直接跑（推荐，方便看日志和调试）
venv/bin/python run_server.py
```

开发环境不挂 PM2，保持前台运行以便观察日志。仓库中的 `ecosystem.config.cjs` 是 prod 配置，进程名为 `funasr-server` 且 `autorestart=true`；不要把它当作 dev 配置启动。

### 跑测试
```bash
# 默认 unit（毫秒级）
venv/bin/python -m pytest

# 端到端 parity（需要加载 FunASR 模型 ~2GB，约 1 分钟）
FUNASR_RUN_INTEGRATION=1 venv/bin/python -m pytest tests/integration/
```

---

## 五、引擎切换 (FunASR ↔ Qwen3-Diarize)

服务器是 **全局唯一引擎模式**: 启动时按 `FUNASR_DEFAULT_ENGINE` (或 `config.transcription.default_engine`) 锁定一个引擎, 不支持运行时切换. upload_request.engine 字段必须与之相同或留空, 不同则 reject.

### 切到 Qwen3-Diarize
```bash
# 1) 落地模型 (~2.1GB, 优先从 ~/Production/qwen_asr_server/ + spike 镜像复制)
bash scripts/download_qwen3_models.sh
bash scripts/download_qwen3_models.sh --verify  # 文件就绪校验

# 2) 切配置。mac_prod 的 Qwen3 provider 是 coreml_ane_full，普通下载脚本不含 backend .mlpackage。
# 方案 A（推荐，复用已下载的 ONNX backend，doctor 不会因 mlpackage 缺失报错）：
echo "FUNASR_DEFAULT_ENGINE=qwen3" >> .env
echo "FUNASR_QWEN3_ASR_ENCODER_PROVIDER=coreml_ane_fe" >> .env
venv/bin/python scripts/doctor.py --json

# 方案 B（仅完成下方 Phase 3 的 .mlpackage 生成后）：保留 mac_prod 的 full provider
echo "FUNASR_DEFAULT_ENGINE=qwen3" >> .env
echo "FUNASR_QWEN3_ASR_ENCODER_PROVIDER=coreml_ane_full" >> .env
venv/bin/python scripts/doctor.py --json

# 3) 重启服务
pm2 restart funasr-server --update-env  # 或 venv/bin/python run_server.py
```

启动日志会显示 `Qwen3-Worker-0` 到 `Qwen3-Worker-(N-1)` 启动（N 为实际 pool size）+ Metal device init, 各 worker `Qwen3-ASR 引擎加载完成` 即就绪.

### Qwen3 多 Worker 池架构 (PR3, 2026-05-15)

Qwen3 引擎走 **multi-process worker pool**, 跟 FunASR 同一套架构, 但物理隔离:
- 池大小独立: FunASR 用 `max_concurrent_tasks=2`, Qwen3 用 `qwen3_pool_size`（profile 默认 1，互不影响）
- 任务目录物理隔离: FunASR 用 `temp/tasks/`, Qwen3 用 `temp/tasks_qwen3/`(避免同机器多 server 抢任务文件)
- 主进程不持有 libllama context, 由每个 worker subprocess 独立加载 GGUF + Metal + sherpa
- worker 内 libllama context per-process 隔离, 解决并发 unsafe(kv cache / Metal compute buffer 串台)

| 引擎 | pool 字段 | 默认值 | 任务目录 | 内存预算 |
|---|---|---|---|---|
| funasr | `max_concurrent_tasks` | 2 | `temp/tasks/` | ~1-2GB per worker |
| qwen3 | `qwen3_pool_size` | **1**（全 profile，2026-06-10 起） | `temp/tasks_qwen3/` | ~3.5GB per worker (GGUF mmap 共享, N=3 总 ~5-7GB) |

**N 选择建议**(PoC v5 实测, 见 `spikes/qwen3_diarize/spike_report.md` 第 5 节):
- **N=1 默认**（2026-06-10 拍板）: 单任务拿满 GPU 延迟最优; 3060 12GB 实测 N=2 + word_align 双 MMS CUDA session 撞显存 OOM（fallback 不挂但词级时间戳静默丢失）
- N=2 快速验证 / 模型加载减半: 适合 dev 测试 / 内存受限场景（CUDA 上开 word_align 时显存需预算 ≥2×MMS）
- N=3 短任务 throughput 优先: GPU 97% 满载, throughput 2.12 task/min (5min 双人音频, Mac 实测)
- **长音频并发 (>10min) RTF 翻倍预警** (2026-05-16 实测): N=2 双 task 长音频并发时单 task RTF 从 ~0.10 升到 ~0.30-0.43, 因 GPU 在两 task 间分时
- N≥4 GPU 100% 饱和: 纯排队, 不会更快

### Qwen3 num_threads 调优 (PoC 验证)

`FUNASR_QWEN3_NUM_THREADS` 同时控 ONNX (ASR encoder) + sherpa-onnx (segmentation/embedding) 的线程池. PoC 实测 (M1 Max 10 cores, pool_size=2) 默认值 8 → 4 后 N=2 wall -11.5%, 单 task RTF 也更好. 原因: t=8 时总 threads = 2×8 = 16 远超物理核数, CPU 过订阅; t=4 时总 threads = 2×4 = 8 ≤ 10 cores 留 2 core 给系统调度.

**实际生产默认值**: `config.json` 显式设置 `num_threads=8`，当前 profile 不覆盖它。M1 Max、`pool_size=2` 的实测调优值是 4；只有显式设置 `FUNASR_QWEN3_NUM_THREADS=4` 才会采用该值。

**自定义公式**: `num_threads ≤ (物理核数 - 2) / pool_size`, 例如:
- 8-core CPU + pool_size=2 → num_threads=3
- 10-core CPU + pool_size=2 → num_threads=4 ✅ (生产推荐)
- 16-core CPU + pool_size=3 → num_threads=4 (或 5 但留更少余量)

详见 `spikes/qwen3_mac_hw_accel/SUMMARY.md` / `spikes/qwen3_mac_hw_accel/num_threads_tuning.md`.

### Qwen3 ASR encoder ANE 加速 (macOS, Phase 2)

macOS Apple Silicon 上 **Qwen3-ASR encoder frontend 自动走 CoreML ANE** (从 Phase 2 起默认开启):

- 路由: `build_engine_config` 在 macOS 上默认 `onnx_provider="COREML_ANE_FE"`, 让 frontend ONNX 走 `CoreMLExecutionProvider` + `MLProgram` format + `CPUAndNeuralEngine` units; backend 保 CPU (axis 4 op CoreML 不兼容)
- 收益: PoC 实测 (M1 Max, 60s 双人) frontend 24.5→10.9s, N=2 wall -7.5%, 跟 num_threads=4 组合 -16.1% (495.6→415.8s)
- ANE 启用: mean 50mW max 336mW; llm_decode 保持 37s 不退化 (units 故意排除 GPU 避免抢 llama.cpp Metal)

**关闭/切换** (生产 escape hatch):
```bash
# 强制 CPU (一键回退, 例如 ANE 出问题)
echo "FUNASR_QWEN3_ASR_ENCODER_PROVIDER=cpu" >> .env

# 显式 frontend ANE (默认值, 非 macOS 自动 fallback CPU)
echo "FUNASR_QWEN3_ASR_ENCODER_PROVIDER=coreml_ane_fe" >> .env

# 自动 (按平台感知, 默认值, 也是 unset 时的行为)
echo "FUNASR_QWEN3_ASR_ENCODER_PROVIDER=auto" >> .env
```

**不要做的事** (PoC 验证负面, 详见 `spikes/qwen3_mac_hw_accel/coreml_asr_encoder.md`):
- `MLComputeUnits=ALL` (会抢 llama.cpp Metal, llm_decode +73s)
- `ModelFormat=NeuralNetwork` (silent fallback CPU, 无收益)
- backend 也走 CoreML EP (卡 axis 4 op) — Phase 3 改用 mlpackage 重导, 见下

### Qwen3 ASR encoder backend ANE/GPU (Phase 3, ⚡ N=2 wall -22.6%)

PyTorch → coremltools → `.mlpackage`, backend 走 CoreML (`COREML_ANE_FULL`),frontend 仍 ANE。

**N=2 实测对比** (M1 Max 64GB, macOS 26.5, 16min+44min 长音频并行):

| 指标 | Phase 1 (全 CPU) | Phase 2 (frontend ANE) | **Phase 3 (full ANE/GPU)** |
|---|---|---|---|
| **wall_total** | 495.6s | 458.0s (-7.5%) | **354.2s (-28.5%)** |
| 4spk-44min wall | — | 457.0s | **346.8s (-24.1%)** |
| 4spk encoder total | — | 84.9s | **18.95s (-77.7%)** |
| 4spk backend | 60.7s (CPU) | 60.7s (CPU) | **5.84s (GPU, -90.4%)** |
| 4spk llm_decode | — | 348.2s | **309.5s (-11%, 意外) |

**硬件利用率** (powermetrics 实测 N=2):
- **CPU**: mean 12.6W, peak 32.9W — 主要 sherpa diarize + Python 协调
- **GPU**: mean 16.9W, peak 44.0W, residency 45% — backend mlpackage + LLM decode 共享 Metal
- **ANE**: peak 299mW — frontend ONNX (mean readout 56mW 偏低, macOS 26 powermetrics bug)
- **不再 CPU 打满 GPU 空等**: 详见 `spikes/qwen3_mac_hw_accel/phase3_backend_mlpackage.md`

**启用** (M1 Max 生产可用,N=2 必须配 GPU backend):
```bash
# 1. 生成 .mlpackage (~3.4GB 下载 + 44s 转换, 一次性)
venv/bin/python -m pip install transformers coremltools pyobjc-core pyobjc-framework-CoreML
venv/bin/python spikes/qwen3_mac_hw_accel/phase3_backend/export_backend_coreml.py --time 520

# 2. 切 Phase 3 + N=2 关键 (backend 走 GPU 不抢 ANE)
echo "FUNASR_QWEN3_ASR_ENCODER_PROVIDER=coreml_ane_full" >> .env
echo "FUNASR_QWEN3_BACKEND_MLPACKAGE_UNITS=CPU_AND_GPU" >> .env  # N=2 必须, N=1 可 CPU_AND_NE
```

**为什么 N=2 必须 backend GPU 不 ANE?**

- N=1: frontend ANE + backend ANE 单 worker = 2 路 ANE 推理, **OK**
- N=2: 双 worker = frontend ANE × 2 + backend ANE × 2 = **4 路 ANE 共享**, 触发 llama.cpp `ggml_abort` (LLM sampler 越界)
- 切 backend GPU 后: ANE 只跑 frontend (2 路), GPU 跑 backend + LLM decode (Metal dispatch queue 共享但未观察到冲突)

**实施细节** (避坑):
- 用 PyObjC + `MLMultiArray.initWithDataPointer + deallocator=None` 零拷贝调 CoreML — 绕开 `coremltools.MLModel.predict` 的 `MLE5ExecutionStream` lingering reset SIGSEGV race (macOS 26+ 已知 bug, 详见 `docs/ref/coreml_lingering_race/`)
- 默认 `auto` 仍走 Phase 2 `coreml_ane_fe` (生产稳健); Phase 3 通过显式 env 启用

详细数据 + 5 份 crash report 分析: `spikes/qwen3_mac_hw_accel/phase3_backend_mlpackage.md`

### Qwen3 引擎 preset 调优
默认 PoC `preset=auto` (普适最优, 自适应聚类). 已知说话人数时锁簇可更精准:
```bash
# 已知 2 人 (固定簇)
echo "FUNASR_QWEN3_NUM_SPEAKERS=2" >> .env

# 未知人数 (默认自适应, threshold 0.9)
echo "FUNASR_QWEN3_NUM_SPEAKERS=auto" >> .env

# 阈值微调 (越大聚出的 speaker 越少, 默认 0.9)
echo "FUNASR_QWEN3_CLUSTER_THRESHOLD=0.85" >> .env
```

详细 preset 对照见 `spikes/qwen3_diarize/spike_report.md` 第 4 节.

### Qwen3 词级时间戳 (word_align, MMS-300M CTC-FA)

给 Qwen3 引擎**增量挂词级时间戳**到 `segment.words`(不替换段边界)。基于 MMS-300M
CTC-FA(`ctc-forced-aligner` ONNX),多语种。**字段默认关闭，四个 profile 均不覆盖该字段**；需通过 per-request `word_align` 或显式环境变量开启（CUDA 仅 +1% RTF，Mac CPU +17% RTF）。JSON 输出才带词
(SRT 不带)。实现见 `CLAUDE.md`「Qwen3 后处理 pipeline」5.5 层。

> `cuda_prod`/`cuda_dev` 不会自动开启 word_align；需要时显式设置
> `FUNASR_QWEN3_WORD_ALIGN_ENABLED=true`，或在请求中传 `word_align=true`。
> 仍须先预下 MMS 模型；CUDA 还须修 onnxruntime-gpu（见下）。

```bash
# 1) 预下 MMS 模型 (~1.2GB, 优先拷 ~/ctc_forced_aligner/model.onnx, 缺则 HF 直下)
bash scripts/download_qwen3_models.sh --word-align
#    落到 models/qwen3_diarize/ctc_forced_aligner/model.onnx

# 2) 开启 + 设兜底语言 (ISO 码: chi/eng/jpn/kor...)
echo "FUNASR_QWEN3_WORD_ALIGN_ENABLED=true" >> .env
echo "FUNASR_QWEN3_WORD_ALIGN_LANGUAGE=chi" >> .env   # per-request language 字段优先, 此为兜底
# CUDA 机器额外指定 provider 走 GPU (Mac 留默认 auto→CPU 即可)
echo "FUNASR_QWEN3_WORD_ALIGN_PROVIDER=cuda" >> .env

# 3) 重启服务
pm2 restart funasr-server --update-env   # 或 venv/bin/python run_server.py
```

- **per-request 语言**:客户端上传时带 `language` 字段(ISO 码)按请求覆盖;缺省走
  `FUNASR_QWEN3_WORD_ALIGN_LANGUAGE` 兜底。中英混排用 `chi`。
- **RTF 代价**:Mac CPU 约 **+17%**(增量 RTF +0.166),RTX 3060 CUDA 仅 **~+1%**
  (增量 RTF +0.011,2026-06-10 实测,见 `spikes/qwen3_word_timestamp/SUMMARY.md`)。
  CUDA 上可放心常开,Mac CPU 需权衡。
- **失败兜底**:某 chunk 对齐失败 → 该段 `words=null`,段照常出,不崩;遥测在
  `raw_result.word_align`(`failed_windows` / `total_words`)。
- **缓存隔离**:开启后缓存 key 自动变 `qwen3+wa:<lang>`,跟未开启 / 不同语言结果不互相命中。

> ⚠️ **CUDA 机器部署陷阱**:`ctc-forced-aligner` 依赖声明的是 **CPU 版 `onnxruntime`**,
> `pip install -r requirements.txt` 会装上它并**覆盖** `onnxruntime-gpu` 的
> `onnxruntime/capi/*.so` → `CUDAExecutionProvider` 消失,服务启动时
> `_validate_engine_runtime` 直接 fail-fast(`default_engine=qwen3 + runtime=cuda` 报错)。
> **CUDA 机器装完依赖后必须修复**:
> ```bash
> venv/bin/pip uninstall -y onnxruntime
> venv/bin/pip install --force-reinstall --no-deps onnxruntime-gpu==<原版本>   # 如 1.26.0
> # 验证: 带 LD_LIBRARY_PATH 跑 python -c "import onnxruntime;print(onnxruntime.get_available_providers())" 应含 CUDAExecutionProvider
> ```
> Mac/CPU 部署无此问题(本就用 CPU onnxruntime)。MMS CUDA + llama.cpp CUDA + ORT diarize
> 三者进程内共存已验证不 segfault(3060 实测)。

---

## 六、常见问题

### 模型下载失败
```bash
# 检查网络（ModelScope 在国内访问通常正常）
curl -I https://www.modelscope.cn/

# 手动触发下载
venv/bin/python -c "from modelscope import snapshot_download; \
  snapshot_download('iic/speech_seaco_paraformer_large_asr_nat-zh-cn-16k-common-vocab8404-pytorch')"

# 模型默认存 ~/.cache/modelscope/hub/，可通过 MODELSCOPE_CACHE 环境变量覆盖
```

### 端口被占用
```bash
lsof -i :8767                              # 看占用进程
pm2 list | grep funasr                     # 看是否是另一个 PM2 实例
```

### 数据库迁移失败（PR1 引入）
- PR1 后首次启动会自动 ALTER TABLE 加 `engine` 列
- 失败时检查 `~/Production/funasr_spk_server/data/transcription_cache.db` 是否锁定
- 极端情况备份后删除数据库重建：`mv data/transcription_cache.db data/transcription_cache.db.bak`

### MPS 长音频转录失败
- 已有补丁：`src/core/patches/mps_patch.py`（2025-10-12 引入）
- 详见 [docs/开发/gpu加速/2025-10-12-MPS长音频故障与补丁方案.md](开发/gpu加速/2025-10-12-MPS长音频故障与补丁方案.md)

---

## 五点五、可观测性告警（launchd 定时拉 /metrics 发企微）

与 server 进程**解耦**的外部告警:脚本周期拉 `/health` + `/metrics`,超阈值发企微。
server 整个挂了也能告警(server 内嵌方案做不到)。纯逻辑在 `src/observability/alerting.py`
(已单测),入口 `scripts/metrics_alert.py`,复用现有通知 webhook。

### 告警规则
| 规则 | 级别 | 触发 | 恢复通知 |
|---|---|---|---|
| `server_down` | critical | `/health` 连不上 或 status≠healthy | 有 |
| `queue_saturation` | warning | `queue_size/queue_max ≥ ratio`(默认 0.8) | 有 |
| `error_surge` | warning | `errors_total` 单轮增量 ≥ 阈值(默认 5) | 无(事件型) |

同一状态型告警 `cooldown_seconds`(默认 900s=15min)内不重复通知。

### 配置(走 `.env`,脚本在项目根自动 `load_dotenv`)
```bash
FUNASR_ALERT_ENABLED=true            # 总开关,默认 false(不开则脚本空跑退出)
FUNASR_WEBHOOK_URL=https://...       # 复用通知 webhook(通常已配)
FUNASR_METRICS_TOKEN=xxx             # host=0.0.0.0 时 /metrics 鉴权必需
# 可选阈值覆盖:
FUNASR_ALERT_QUEUE_SATURATION_RATIO=0.8
FUNASR_ALERT_ERROR_SURGE_THRESHOLD=5
FUNASR_ALERT_COOLDOWN_SECONDS=900
```
state 默认落 DB 同目录 `alert_state.json`,`FUNASR_ALERT_STATE_PATH` 可覆盖。

### 手动验证
```bash
# server 在跑时,单跑一次看是否能拉到指标(无告警则静默)
FUNASR_ALERT_ENABLED=true venv/bin/python scripts/metrics_alert.py
```

### 安装为 launchd 定时任务(prod)
```bash
PROJ=$HOME/Production/funasr_spk_server
sed "s#__PROJECT_ROOT__#$PROJ#g" $PROJ/scripts/launchd/com.funasr.metrics-alert.plist \
  > ~/Library/LaunchAgents/com.funasr.metrics-alert.plist
launchctl load -w ~/Library/LaunchAgents/com.funasr.metrics-alert.plist
# 停用: launchctl unload -w ~/Library/LaunchAgents/com.funasr.metrics-alert.plist
```
日志在 `logs/metrics-alert.log`,轮询间隔默认 60s(改 plist 的 `StartInterval`)。

---

## 六、参考

- ASR 引擎架构（FunASR + Qwen3 双引擎现状）：项目根 [CLAUDE.md](../CLAUDE.md) ASR 引擎章节
- 客户端 WebSocket 协议：[docs/使用/客户端交互指南.md](使用/客户端交互指南.md)
- 服务端交互协议：[docs/开发/Server-Client 交互协议.md](开发/Server-Client%20交互协议.md)
- MPS 加速实施总结：[docs/开发/gpu加速/2025-10-09-MPS加速方案实施总结.md](开发/gpu加速/2025-10-09-MPS加速方案实施总结.md)
- 项目根 [CLAUDE.md](../CLAUDE.md) 部署约定章节
