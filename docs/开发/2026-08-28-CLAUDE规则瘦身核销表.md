# 2026-08-28 CLAUDE.md 规则瘦身核销表（初稿）

- 源文件：仓根 `CLAUDE.md`（基线 `f0c9e13`，37363B / 368 行）
- 配方：`agent-config/docs/guides/rules-budget.md` 三问准入
  1. 这条每个会话都需要吗？
  2. 能从代码 / README / git log 推断吗？
  3. 现在还成立吗？
- 不全过 → 不进仓根 `AGENTS.md`。处置三选一：保留 / 搬 docs 留指针 / 删（必须写明被哪个文档或事实覆盖）。
- 终态：`AGENTS.md` 唯一真身且 ≤8192B；`CLAUDE.md` 恰为一行 `@AGENTS.md`。

## 对账

| 口径 | 数量 | 说明 |
|---|---|---|
| `grep -nE '^#{1,3} '` 命中 | 46 | 含代码块内 shell 注释 |
| 其中代码块伪标题 | 5 | 行 205/208/220/231/234，非独立条目，见附录 |
| 真实 H1 | 8 | 代码设计 / 测试约定 / ASR 引擎架构 / Config 体系 / 任务队列 / 可观测性 / DB 缓存 / 部署约定 |
| 真实 H2 | 26 | 核销表必覆盖 |
| 真实 H3 | 7 | 核销表必覆盖 |
| 无标题前言 | 1 | `Always respond in 中文` |
| **核销表条目** | **42** | 前言 1 + 真实标题 41（H1+H2+H3） |
| 二级+三级标题 | 33 | 与真实 H2+H3 一致，无遗漏 |

## 核销表

| # | 原条目 | 级 | 行 | 处置 | 理由 |
|---|---|---|---|---|---|
| 0 | Always respond in 中文 | 前言 | 1 | 保留 | 三问全过：每个会话需要；代码推断不出；仍成立。 |
| 1 | 代码设计上的要求 | H1 | 3 | 保留 | 三问全过：工程底线，每个会话写代码都要；非代码可推断的风格纪律。 |
| 2 | 测试约定（PR1 之后启用） | H1 | 11 | 保留 | 父节标题，与下三条测试纪律同命运。正文几乎只有子节，排版级保留标题。 |
| 3 | pytest 套件 | H2 | 13 | 保留 | 三问全过：目录分层 / `-m pytest` / integration skip 是每个写测试会话的入口约束。 |
| 4 | TDD 流程 | H2 | 21 | 保留 | 三问全过：红绿提交纪律无法从代码推断。 |
| 5 | Parity 测试 | H2 | 26 | 保留 | 三问全过：改 FunASR 路径必跑的闸，漏了会静默破坏生产引擎。 |
| 6 | ASR 引擎架构 | H1 | 34 | 保留 | 父节标题保留；子节按下列条目分别处置。仓根只留硬约束 + 指针，不复述带日期叙事。 |
| 7 | 当前现状（2026-05-23） | H2 | 36 | 搬 docs 留指针 | Q1 不过：完整双引擎现状叙事不是每个会话都要读完。Q2：路径与 dispatch 可从代码推断。全文原样搬 `docs/开发/ASR引擎架构.md`；仓根只留一行指针。硬约束「不是 ABC / 引擎优先级 / 缓存按 (file_hash, engine) 隔离」与「关键架构决策」合并排版保留（不改语义）。 |
| 8 | Runtime + Diarize backend 抽象 | H2 | 46 | 搬 docs 留指针 | Q1 不过：仅改 runtime/diarize backend 时需要。Q2：`src/core/runtime.py` + `diarize.py` 即真相。全文原样搬 `docs/开发/ASR引擎架构.md`。已有定案不新建重复：`docs/开发/gpu加速/2026-05-22-ORT-CUDA-diarize-backend.md`、`docs/开发/2026-06-10-ort_cuda短音频under-detect-修复结案.md`。 |
| 9 | Qwen3 池 dispatch (runtime-aware) | H2 | 60 | 搬 docs 留指针 | Q1 不过。Q2：`qwen3_pool_transcriber.py` / `qwen3_inproc_pool.py` 即真相。全文原样搬 `docs/开发/ASR引擎架构.md`。已有定案：`docs/开发/gpu加速/2026-05-23-CUDA并发突破.md`。 |
| 10 | Qwen3 后处理 pipeline | H2 | 77 | 搬 docs 留指针 | Q1 不过：8KB 层序叙事不是每个会话的入场券。Q2：`Qwen3DiarizeTranscriber.transcribe` 即真相。Q3 仍成立故不删。全文原样搬 `docs/开发/ASR引擎架构.md`。子题 word_align 已有定案不新建：`docs/开发/gpu加速/2026-06-16-Qwen3-word-align显存PoC与落地计划.md`、`docs/开发/gpu加速/2026-06-16-word-align显存安全-评审定案与落地计划.md`。 |
| 11 | diarize 开关（2026-06-10 落地，定案: `docs/开发/2026-06-10-diarize开关API-设计定案.md`） | H2 | 101 | 搬 docs 留指针 | Q1 不过：带日期定案叙事。文内已引用定案，**不新建第二份定案**。CLAUDE 正文（含 issue #1 merge、nospk、缓存折维）与定案不完全同一份文字，故**原样搬入** `docs/开发/ASR引擎架构.md` 作规则摘录，仓根只留一行指向定案 + 摘录。 |
| 12 | ⚠️ Qwen3 worker audio 转换的边界 | H3 | 116 | 保留 | 三问全过：踩过的坑（ffmpeg 转 wav 触发 over-detect），代码里「跳过转换」看起来像不一致，每个改 worker/音频路径的会话都需要。**存疑，主脑复核**（是否应改搬 docs）：执行器按高风险区域「拿不准保留」处理。 |
| 13 | 关键架构决策 | H2 | 120 | 保留 | 高风险区域。Q1 倾向过：阻止再引入 ABC/factory。Q2 不能从代码「看出不要抽象」。**存疑，主脑复核**。 |
| 14 | 加新引擎的步骤 | H2 | 126 | 保留 | 高风险区域。新引擎接入清单每个相关会话需要，且不是代码能推断的流程。**存疑，主脑复核**。 |
| 15 | 加新 diarize backend 的步骤 | H2 | 132 | 保留 | 同上，高风险区域，保留。**存疑，主脑复核**。 |
| 16 | Config 体系（2026-05-22 治理后） | H1 | 138 | 保留 | 父节标题保留。仓根只留优先级链 + 加字段步骤；其余搬 docs。 |
| 17 | 优先级链 | H2 | 142 | 保留 | 三问全过：defaults < config.json < PROFILE < env 是改配置时必知、且不能单看 `config.json` 推断的覆盖顺序。 |
| 18 | FUNASR_PROFILE 套餐 env | H2 | 150 | 搬 docs 留指针 | Q1 不过：套餐表是操作手册。Q2：`src/core/config.py` 的 `PROFILES` 即真相。全文原样搬 `docs/开发/Config体系.md`。 |
| 19 | "auto" sentinel 字段 (Pydantic model_validator 解析) | H2 | 167 | 搬 docs 留指针 | Q1 不过。Q2：`Qwen3Config` model_validator 即真相。全文原样搬 `docs/开发/Config体系.md`。 |
| 20 | vendor 字段进 Pydantic | H2 | 181 | 搬 docs 留指针 | Q1 不过。Q2：代码即真相（「不再 `os.environ.get`」已落地）。全文原样搬 `docs/开发/Config体系.md`。 |
| 21 | startup engine-runtime fail-fast | H2 | 190 | 搬 docs 留指针 | Q1 不过：仅改启动/默认引擎时需要。Q2：`Config._validate_engine_runtime` 即真相。全文原样搬 `docs/开发/Config体系.md`。 |
| 22 | 切换设备 / 切引擎操作手册 | H2 | 196 | 搬 docs 留指针 | Q1 不过：操作手册全文。Q2 部分已在 `docs/部署.md` 第五节。CLAUDE 正文与部署文档不是逐字同一份，故原样搬 `docs/开发/Config体系.md`；仓根指针同时指向 `docs/部署.md`。不改写 `docs/部署.md` 既有段落。 |
| 23 | A. Mac + FunASR (主路径, 速度快 / 并发好) | H3 | 200 | 搬 docs 留指针 | 随父节「切换设备 / 切引擎操作手册」原样搬 `docs/开发/Config体系.md`。Q1 不过。 |
| 24 | B. Linux CUDA + Qwen3 (远端 dev box / 未来 prod) | H3 | 214 | 搬 docs 留指针 | 同上。 |
| 25 | C. Mac + Qwen3 (追准确度, 按需切) | H3 | 226 | 搬 docs 留指针 | 同上。 |
| 26 | "改哪里" 决策树 | H3 | 240 | 搬 docs 留指针 | 同上。Q1 不过（偶发操作说明）。 |
| 27 | 最常见 3 种用法 | H3 | 251 | 搬 docs 留指针 | 同上。 |
| 28 | 启动后怎么验证配置生效 | H3 | 272 | 搬 docs 留指针 | 同上。 |
| 29 | 加 config 字段的步骤 | H2 | 284 | 保留 | 三问全过：加字段的五步清单是流程纪律，不是单文件能推断的「下次也要改哪几处」。 |
| 30 | 任务队列与高负载健壮性（2026-06-16 止血） | H1 | 292 | 搬 docs 留指针 | Q1 不过：带日期止血叙事。已有定案 **不新建重复**：`docs/开发/2026-06-16-高负载队列机制-止血修复计划.md`。CLAUDE 正文原样搬入 `docs/开发/任务队列与可观测性.md` 作规则摘录。仓根只留一行指针 + 一条硬约束（队列满必须 `self.tasks.pop` 回滚），该硬约束从本父节及「self.tasks 内存清理」排版级抽出，不改语义。 |
| 31 | 异步轮询契约 task_status_batch(TODOS #20，2026-06-16 服务端落地) | H2 | 296 | 搬 docs 留指针 | Q1 不过。已有定案：`docs/开发/2026-06-16-异步轮询契约-设计定案与落地计划.md`。CLAUDE 正文原样搬 `docs/开发/任务队列与可观测性.md`。 |
| 32 | 队列 = 准入控制,不是缓冲池 | H2 | 299 | 搬 docs 留指针 | Q1 不过（实现复述）。Q2：`TaskManager.task_queue` 即真相。全文原样搬 `docs/开发/任务队列与可观测性.md`。硬约束「满则 `QueueFullError`」随父节在仓根留一行指针级摘要，不另改语义。 |
| 33 | self.tasks 内存清理(TTL + size-cap 双保险) | H2 | 302 | 搬 docs 留指针 | Q1 不过。Q2：`_evict_terminal_tasks` 等即真相。全文原样搬 `docs/开发/任务队列与可观测性.md`。codex 窟窿（先写 `self.tasks` 再查容量）作为硬约束排版并入仓根队列节一行。 |
| 34 | 孤儿上传文件 sweeper(P4 A2,2026-06-17) | H2 | 309 | 搬 docs 留指针 | Q1 不过。已有定案：`docs/开发/2026-06-17-P4小硬化-A1缓存sizecap-A2孤儿文件-落地计划.md`。CLAUDE 正文原样搬 `docs/开发/任务队列与可观测性.md`。 |
| 35 | 分片 session 资源处理(`websocket_handler.py`) | H2 | 313 | 搬 docs 留指针 | Q1 不过。Q2：`websocket_handler.py` 即真相。全文原样搬 `docs/开发/任务队列与可观测性.md`。 |
| 36 | EMA 处理时长 | H2 | 319 | 搬 docs 留指针 | Q1 不过。Q2：代码即真相。全文原样搬 `docs/开发/任务队列与可观测性.md`。 |
| 37 | 可观测性仪表盘（P1，2026-06-16） | H1 | 322 | 搬 docs 留指针 | Q1 不过：定案叙事 + 实现复述。已有定案不新建：`docs/开发/2026-06-16-可观测性仪表盘与测试加固-设计定案与落地计划.md`。CLAUDE 正文原样搬 `docs/开发/任务队列与可观测性.md`。硬约束「见 `Upgrade: websocket` 头一律 `return None`」三问全过（生产事故 2026-06-17，改 HTTP 端点每个会话都要），排版级保留该段于仓根，不改语义；事故 retro：`docs/开发/2026-06-17-根路径ws握手被HTML状态页劫持-事故retro.md`。 |
| 38 | DB 缓存清理(P4 A1,2026-06-17) | H1 | 338 | 搬 docs 留指针 | Q1 不过。已有定案：`docs/开发/2026-06-17-P4小硬化-A1缓存sizecap-A2孤儿文件-落地计划.md`。CLAUDE 正文原样搬 `docs/开发/任务队列与可观测性.md`。硬约束「cutoff 必须 `strftime`，禁用 `isoformat()`」三问全过（改过会误删），排版级保留一行于仓根。 |
| 39 | 部署约定（macOS only） | H1 | 342 | 保留（硬约束）+ 操作搬指针 | 硬约束「不要调用全局 `docker-deploy` skill」三问全过：`docs/部署.md` **没有**这句话，全局 skill 误触发是会话级陷阱。prod/dev 隔离表与启停命令 Q1 不过、且已被 `docs/部署.md` 覆盖 → 操作部分不在仓根复述，只留指针。CLAUDE 部署节正文仍原样搬入 `docs/开发/Config体系.md`（与 `docs/部署.md` 非逐字同一份，避免信息丢失）；**不改写** `docs/部署.md` 既有段落。 |
| 40 | prod 部署 | H2 | 353 | 搬 docs 留指针 | Q1 不过。已被 `docs/部署.md`「三、生产部署」覆盖（更完整）。CLAUDE 短命令块原样搬 `docs/开发/Config体系.md` 以便抽样逐字对得上。 |
| 41 | dev 运行 | H2 | 361 | 搬 docs 留指针 | Q1 不过。已被 `docs/部署.md`「四、开发运行」覆盖。CLAUDE 短命令块原样搬 `docs/开发/Config体系.md`。 |

## 拟新建文档（本卡后续提交，初稿阶段尚未落盘）

| 新文件 | 承接的原条目 |
|---|---|
| `docs/开发/ASR引擎架构.md` | #7 #8 #9 #10 #11（规则摘录，定案仍以既有日期文档为准） |
| `docs/开发/Config体系.md` | #18–#28、#39 操作部分、#40 #41 |
| `docs/开发/任务队列与可观测性.md` | #30–#38 |

不新建、只指针的既有定案：diarize 开关 API、ORT-CUDA backend、CUDA 并发突破、word-align 两份、高负载队列、异步轮询、P4 小硬化、可观测性仪表盘、根路径 ws 事故 retro、`docs/部署.md`、`docs/开发/archive/重构计划-ASR引擎抽象.md`。

## 拟保留进 `AGENTS.md` 的条目（排版级合并，不改语义）

- #0 中文、#1 代码设计、#2–#5 测试约定全文
- #6 标题 + #7 中「不是 ABC / 引擎优先级 / 缓存隔离」要点（来自保留硬约束，与 #13 并列）
- #12 worker audio 边界全文
- #13 #14 #15 全文（存疑保留）
- #16 标题 + #17 优先级链全文 + #29 加 config 字段步骤全文
- #30/#32/#33 抽出的队列满回滚一行
- #37 抽出的 `Upgrade: websocket` 铁律段
- #38 抽出的 SQLite 时间戳铁律一行
- #39 `docker-deploy` 禁令 + 指向 `docs/部署.md`

其余全部不进仓根，只留一行路径指针。

## 附录：代码块伪标题（不计入 33 条二级/三级对账）

这 5 行会被 `^#{1,3} ` 命中，但是「切换设备 / 切引擎操作手册」代码块里的 shell 注释，不是独立规则条目：

| 行 | 文本 | 处置 |
|---|---|---|
| 205 | `# prod (PM2 守护)` | 随 #23 搬移，不单独核销 |
| 208 | `# dev (前台直跑, 看日志)` | 随 #23 搬移 |
| 220 | `# LD_LIBRARY_PATH 配 CUDA libs ...` | 随 #24 搬移 |
| 231 | `# 干净法 (走 config.json 默认 port/log)` | 随 #25 搬移 |
| 234 | `# 套餐 + 覆盖法 ...` | 随 #25 搬移 |
